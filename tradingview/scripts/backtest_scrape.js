// backtest_scrape.js — extrait les stats du Strategy Tester TradingView (DOM scrape).
// Usage : node src/cli/index.js ui eval "$(cat backtest_scrape_expr.js)"  (ou via le driver bash)
// Renvoie l'innerText du conteneur de rapport ; le parsing se fait côté Python.
