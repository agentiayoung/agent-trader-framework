# Normalize line endings: LF in the repo, auto on checkout.
* text=auto eol=lf

# Windows scripts must stay CRLF (PowerShell 5.1 / cmd).
*.ps1 text eol=crlf
*.bat text eol=crlf

# Treat these as binary (never diff/merge).
*.png binary
*.jpg binary
*.pdf binary
